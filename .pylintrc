# .pylintrc
#
# Конфігураційний файл для Pylint.
# Допомагає налаштувати правила статичного аналізу коду Python.

[MASTER]
# Шляхи, які Pylint має ігнорувати під час сканування.
# Це корисно для виключення віртуальних середовищ, папок з документацією тощо.
ignore=.venv,docs,tests,.git,__pycache__,*.pyc

[MESSAGES CONTROL]
# Вимкнення конкретних повідомлень Pylint за їхніми кодами.
# W0613 (unused-argument): Часто виникає у фреймворках (як python-telegram-bot),
# де аргументи (наприклад, 'context') є частиною сигнатури функції,
# але не завжди використовуються безпосередньо в тілі функції.
disable=W0613

[BASIC]
# Список поганих імен змінних, яких слід уникати.
bad-names=i,j,k,l,m,n,O,x,y,z,foo,bar,baz
# Список імен, які вважаються прийнятними, навіть якщо вони короткі або специфічні.
# Це допомагає Pylint не позначати стандартні аргументи фреймворків як "погані імена".
good-names=app,context,idx # Додано 'idx' як прийнятне коротке ім'я для індексів
# Регулярний вираз для перевірки імен змінних.
# Дозволяє короткі імена, які є у good-names
variable-rgx=[a-z_][a-z0-9_]{1,30}$
# Регулярний вираз для перевірки імен констант.
const-rgx=(([A-Z_][A-Z0-9_]*)|(__.*__))$
# Регулярний вираз для перевірки імен функцій.
function-rgx=[a-z_][a-z0-9_]{2,30}$
# Регулярний вираз для перевірки імен методів.
method-rgx=[a-z_][a-z0-9_]{2,30}$
# Регулярний вираз для перевірки імен класів.
class-rgx=[A-Z_][a-zA-Z0-9_]{2,30}$
# Регулярний вираз для перевірки імен аргументів.
argument-rgx=[a-z_][a-z0-9_]{1,30}$
# Регулярний вираз для перевірки імен модулів.
module-rgx=([a-z_][a-z0-9_]*)$

[FORMAT]
# Максимальна дозволена довжина рядка.
max-line-length=120
# Максимальна кількість рядків у модулі.
max-module-lines=1000
# Рядок, що використовується для відступу (4 пробіли).
indent-string='    '
# Максимальна кількість рядків у функції.
# Ця опція не розпізнається деякими версіями Pylint, тому її видалено.
# max-function-lines=50

[REPORTS]
# Деактивуємо відображення зведення та оцінки, якщо вони не потрібні у виводі.
# disable=no-score,no-summary,no-files-and-directories

[TYPECHECK]
# Члени, які встановлюються динамічно та не визначаються Pylint'ом під час інференції.
# Якщо виникають E1101 (no-member) помилки для легітимних динамічних членів, додайте їх сюди.
# generated-members=

[MISCELLANEOUS]
# Список додаткових імен для ігнорування при пошуку невикористаних змінних.
# ignored-variable-names=_
# Список додаткових імен для ігнорування при пошуку невикористаних аргументів.
# ignored-argument-names=_
